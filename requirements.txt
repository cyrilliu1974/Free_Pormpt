# 執行 hybrid_search.py / streamlit_app.py 所需依賴
# 安裝建議在專案 venv 內執行:  pip install -r requirements.txt
# 已在 streamlit_venv 驗證版本: streamlit 1.61.1 / fastembed 0.8.0 / numpy 2.5.2

streamlit>=1.61.0
fastembed>=0.8.0
numpy>=2.5.0
